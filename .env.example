VITE_FE_PORT=9100

# 可选：静态资源和 Module Federation 文件的公共访问前缀；默认生产环境为 /
VITE_CAM_PUBLIC_BASE_URL=/

# 原始后端 Base URL（被 Vite/Caddy 代理至 /api）
CAM_UPSTREAM_BASE_URL=<cam_upstream_base_url>

# 用于从 GitHub 仓库拉取 @cloud-materials/common 组件库
CLOUD_MATERIALS_GITHUB_TOKEN=<github_token>
